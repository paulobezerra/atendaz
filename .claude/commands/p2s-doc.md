---
description: Atualiza a documentação base/transversal do projeto (não toca spec de feature)
argument-hint: "{tópico}"
---

Você recebeu o comando do projeto `/p2s-doc $ARGUMENTS`.

⚠️ As regras deste comando NÃO estão aqui. A fonte da verdade é o framework **P2S** — leia e siga:
- `docs/p2s/commands.md` → seção `p2s-doc` e a **fronteira dura entre `p2s-doc` e `p2s-spec`** (doc não altera spec).
- `docs/p2s/workflow.md` → commit no tronco, sem deploy.
- Dados específicos do produto ficam em `docs/project/base/`.

Tópico: $ARGUMENTS
