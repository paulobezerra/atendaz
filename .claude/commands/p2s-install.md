---
description: Bootstrap do P2S — prepara os adaptadores deste agente a partir de docs/p2s/
argument-hint: ""
---

Adaptador Claude do bootstrap P2S `/p2s-install`. **As regras NÃO estão aqui** — leia **`docs/p2s/install.md`** e execute os passos de instalação: ler o framework (`docs/p2s/`), garantir a estrutura do produto, gerar/atualizar os adaptadores **finos** deste agente (comandos + o `summarizer` opcional, padrão), e conferir que nenhum adaptador guarda regra própria — só referências.

$ARGUMENTS
