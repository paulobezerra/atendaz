---
description: Plano da feature (cenários BDD + inventário cria/altera/exclui) — sem código
argument-hint: "{ID} [contexto]"
---

Adaptador Claude do comando P2S `/p2s-plan`. **As regras NÃO estão aqui** — a fonte da verdade é o framework, agnóstico de agente: leia **`docs/p2s/commands.md` → seção `p2s-plan`** (e `## DOR & DOD`) e siga.

Argumento (ID da feature + contexto): $ARGUMENTS
