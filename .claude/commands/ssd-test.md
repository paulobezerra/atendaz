---
description: Executa os testes em um ambiente — local (Jest), stage (Preview) ou prod
argument-hint: "local|stage|prod"
---

Você recebeu o comando do projeto `/ssd-test $ARGUMENTS`.

⚠️ As regras deste comando NÃO estão aqui. A fonte da verdade é a documentação do projeto — leia e siga:
- `docs/00-agent-instructions.md` → seção "Comandos de Mini Agentes" (`/ssd-test`) e "Fluxo de Branches, Ambientes e Deploy".

Ambiente (local | stage | prod): $ARGUMENTS
