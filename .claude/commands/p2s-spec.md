---
description: Spec da feature via prototipação navegável (UX, API, dados, integrações)
argument-hint: "{ID} [contexto]"
---

Adaptador Claude do comando P2S `/p2s-spec`. **As regras NÃO estão aqui** — a fonte da verdade é o framework, agnóstico de agente: leia **`docs/p2s/commands.md` → seção `p2s-spec`** e siga.

Argumento (ID da feature + contexto): $ARGUMENTS
