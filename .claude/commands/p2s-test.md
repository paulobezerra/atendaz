---
description: Executa os testes em um ambiente — local (Jest), stage (Preview) ou prod
argument-hint: "local|stage|prod"
---

Você recebeu o comando do projeto `/p2s-test $ARGUMENTS`.

⚠️ As regras deste comando NÃO estão aqui. A fonte da verdade é o framework **P2S** — leia e siga:
- `docs/p2s/commands.md` → seção `p2s-test` ("verde" só conta se cobrir a superfície alterada).
- `docs/p2s/workflow.md` → os três ambientes (local / stage / prod) e o que cada um responde.
- `docs/p2s/quality.md` → política de testes (três camadas).

Ambiente (local | stage | prod): $ARGUMENTS
