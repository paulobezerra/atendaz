---
description: Fechamento lógico (arquiva, marca concluído); promoção ao git flow do projeto
argument-hint: "{ID}"
---

Adaptador Claude do comando P2S `/p2s-done`. **As regras NÃO estão aqui** — a fonte da verdade é o framework, agnóstico de agente: leia **`docs/p2s/commands.md` → seção `p2s-done`** e siga. A IA nunca mergeia o tronco por conta própria (git flow em `docs/project/base/workflow.md`).

Argumento (ID da feature): $ARGUMENTS
