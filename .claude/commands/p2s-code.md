---
description: Implementação da feature (TDD) no git flow do projeto
argument-hint: "{ID}"
---

Adaptador Claude do comando P2S `/p2s-code`. **As regras NÃO estão aqui** — a fonte da verdade é o framework, agnóstico de agente: leia **`docs/p2s/commands.md` → seção `p2s-code`** e siga.

Argumento (ID da feature): $ARGUMENTS
