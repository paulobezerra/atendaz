---
description: QA — testes (script) + revisão holística; achados por severidade
argument-hint: "{ID}"
---

Adaptador Claude do comando P2S `/p2s-review`. **As regras NÃO estão aqui** — a fonte da verdade é o framework, agnóstico de agente: leia **`docs/p2s/commands.md` → seção `p2s-review`** e siga. Delegue as varreduras amplas ao subagente `summarizer` (`docs/p2s/agents.md#summarizer`).

Argumento (ID da feature): $ARGUMENTS
