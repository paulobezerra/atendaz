# Review: F0003.1 — Perfil do profissional

> `p2s-review` · branch `feature/0003.1-perfil` · 2026-07-09.
> Registro histórico (descartável — arquivável após o `p2s-done`, ver
> [ciclo de vida](../../../p2s/workflow.md#ciclo-de-vida-dos-artefatos-descartáveis)).

## Evidência

- Testes: **95/95** verdes (integração de foto/perfil + RTL). Build de produção limpo. `npm audit
  --omit=dev` = **0**. Foto validada em produção (upload/remoção) no gate de revisão humana.
- Núcleo consistente: dados (`redesSociais`), API (PATCH perfil + POST/DELETE foto), UX (abas;
  Perfil restrita ao módulo agenda), isolamento por `businessId`, validação de foto no servidor,
  integração Vercel Blob fiel à doc oficial.

## Achados & desfecho

| # | Severidade | Achado | Desfecho |
| :- | :- | :- | :- |
| 1 | Major (era "bloqueante") | `bio` não estava restrita ao módulo agenda (só `redesSociais` estava). | **Corrigido** — `bio` agora restrita junto com `redesSociais` (404 sem agenda). Reverteu a decisão anterior de "bio livre": como a bio vive só na aba Perfil (restrita), deixá-la aberta só na API era inconsistente e contrariava spec/contrato. |
| 2 | Medium | Faltava teste de `PATCH bio` sem agenda → 404. | **Corrigido** — teste adicionado. |
| 3 | Medium | Faltava teste de `DELETE foto` sem agenda → 404. | **Corrigido** — teste adicionado. |
| 4 | Minor | `DELETE` seta `fotoUrl = undefined` (não `null`). | **Aceito** — `undefined` **desfaz** o campo (idiomático no Mongoose) e o serializer devolve `null`; setar `null` quebraria o tipo `fotoUrl?: string`. |
| 5 | Minor | Audit do PATCH não detalhava alteração de perfil. | **Corrigido** — flag `perfilAlterado` no `payloadResumido`. |
| 6 | Minor | RTL do `PerfilTab` não cobria o ramo de erro do PATCH. | **Corrigido** — teste de erro adicionado. |

**Bloqueantes: 0.** Todos os achados corrigidos (menos o #4, aceito com justificativa).

## Conclusão

Sem pendências. Spec, contrato e código consistentes (bio e redes restritas ao módulo agenda).
Pronto para o `p2s-done` após o portão de revisão humana (foto já validada em produção no Preview).
