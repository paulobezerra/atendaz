# Review: F0002.8 — Configurações + Meio de Pagamento e NFS-e

> `p2s-review` · branch `feature/0002.8-settings-payment` · 2026-07-09.
> Registro histórico (descartável — arquivável após o `p2s-done`, ver
> [ciclo de vida](../../../p2s/workflow.md#ciclo-de-vida-dos-artefatos-descartáveis)).

## Evidência

- Testes: **78/78** verdes (integração 11 + RTL 7 desta feature). Build de produção limpo
  (TypeScript ok). `npm audit --omit=dev` = **0**.
- Aderência confirmada: **D1** (404 por habilitação), **D2** (só token), **D3** (só atualizar);
  guardrails de isolamento (`businessId`), criptografia AES-256-GCM, chave nunca retornada, audit
  sem a chave, reuso de `validateAsaasKey` (sem inventar endpoint Asaas). Cobertura BDD de
  API/segurança/isolamento: completa.

## Achados & desfecho

| # | Severidade | Achado | Onde | Desfecho |
| :- | :- | :- | :- | :- |
| 1 | Medium | Validação **onBlur** não implementada (plano T7) | `PaymentForm.tsx` (`useForm`) | **Corrigido** — `mode: "onBlur"` |
| 2 | Medium | Teste RTL sem cobrir o ramo de **erro** do POST | `tests/components/PaymentForm.test.tsx` | **Corrigido** — +2 testes (erro + onBlur) |
| 3 | Minor | `POST 400` retorna `{error, issues}`; OpenAPI só descreve `{error}` | `route.ts:51` | **Aceito** — superset compatível; segue a convenção da rota `professionals` |
| 4 | Minor | Toast de sucesso/erro (extra) | `PaymentForm.tsx` | **Aceito** — boa UX |
| 5 | Minor | Erro inline (extra) | `PaymentForm.tsx` | **Aceito** — boa UX |
| 6 | Minor | Máscara `•••• last4` (vs ASCII da spec) | `PaymentForm.tsx` | **Aceito** — padrão de cartão, funcional |

**Bloqueantes: 0. Major: 0.** Os 2 medium foram corrigidos; os minor foram aceitos (extras
inofensivos / detalhe de doc do protótipo descartável).

## Conclusão

Sem bloqueantes → o `p2s-done` não está travado. Pendente apenas o **portão de revisão humana**
(teste manual no Preview + revisão do diff) antes do `p2s-done`.
